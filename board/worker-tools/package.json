{
  "name": "@questboard/worker-tools",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "type": "module",
  "bin": {
    "questboard": "./bin/questboard.mjs"
  },
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@questboard/core": "workspace:*",
    "commander": "^12.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.4"
  }
}
